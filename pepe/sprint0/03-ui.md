# S0-3 · UI

Refs: `assets/wireframes.png`, [`edgeCases.md`](edgeCases.md) (stale token).

## Goal

Login → dashboard (profile + balance) → edit details. Mobile-first. `NEXT_PUBLIC_API_URL=/api`.

## Logout (server vs client)

S0-2 ships `POST /api/auth/logout` → `{ ok: true }` with a valid Bearer token. That does **not** revoke the JWT server-side.

**Meaningful logout is implemented here (S0-3), not in the API:** after optional POST logout, the client must `localStorage` remove the token and redirect `/login`. Same for stale tokens: `apiFetch` on **401** clears storage and sends the user to `/login` ([`edgeCases.md`](edgeCases.md)).

## Checklist

- [x] `client/lib/apiFetch.js` — base URL, Bearer from localStorage, 401 → clear token + redirect `/login`
- [x] `client/lib/auth.js` — `getToken` / `setToken` / `clearToken` (`smartpump_token`, SSR-safe)
- [x] `app/login/page` — email/password → POST login → store token → redirect `/`; already-authed → redirect `/`
- [x] Home guard — no token → `/login`; on load GET `/api/me`
- [x] Dashboard — name, email, balance (wireframe fields)
- [x] Edit form — PATCH allowed fields (`name`, `phone`, `address`, `age`, `company`, `eyeColor`), merges API response
- [x] Edit validation — on Save, **every** editable field must be non-blank (trim); `age` must be a positive integer; block submit + inline error (see [`PLAN.md`](PLAN.md) PATCH value rules); hide email while editing
- [x] Logout — optional POST `/api/auth/logout`, always `clearToken()` + redirect `/login`
- [x] Strip Next starter boilerplate from `app/page.js`
- [x] Layout/spacing vs wireframes (mobile first, dark-glass style; wireframe = inspiration)
- [x] Smart Pump branding — `client/public/logo.png`, metadata title, `smartpump_token`
- [x] E2E on `http://localhost:82` — `make test-e2e` (Playwright: login → balance → edit phone → reload → logout)

## Pre-next-phase (before S0-4)

These items were deferred from S0-3 by design. Complete before closing the sprint if time allows:

- [x] **Hamburger / side drawer** — did not like it
- [x] **Separate `/edit` route** — did not like it (for this is more minimalistic no clutter for this exercise)
- [x] **Client unit tests** — `client/test/auth.test.js` + `client/test/apiFetch.test.js` (`make test-client`)

## Done when

Reviewer can complete assignment flows in browser without curl.
