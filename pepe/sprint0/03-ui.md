# S0-3 · UI

Refs: `assets/wireframes.png`, [`edgeCases.md`](edgeCases.md) (stale token).

## Goal

Login → dashboard (profile + balance) → edit details. Mobile-first. `NEXT_PUBLIC_API_URL=/api`.

## Checklist

- [ ] `client/lib/apiFetch.js` — base URL, Bearer from localStorage, 401 → clear token + redirect `/login`
- [ ] `app/login/page` — email/password → POST login → store token → redirect `/`
- [ ] Home guard — no token → `/login`; on load GET `/api/me`
- [ ] Dashboard — name, email, balance (wireframe fields)
- [ ] Edit form — PATCH allowed fields, refetch or merge response
- [ ] Logout — clear storage, go `/login`
- [ ] Strip Next starter boilerplate from `app/page.js`
- [ ] Layout/spacing vs wireframes (mobile first, `sm:` where needed)
- [ ] Manual E2E on `http://localhost:82`: login → see balance → edit → reload still authed

## Done when

Reviewer can complete assignment flows in browser without curl.
